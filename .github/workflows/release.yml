name: Release

# Tag-only: publishing is irreversible, so this never runs on PRs or branch pushes.
on:
  push:
    tags:
      - 'v*'

permissions:
  contents: read
  id-token: write # OIDC: npm Trusted Publishing auth + provenance attestation

jobs:
  publish:
    name: Publish to npm
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          registry-url: https://registry.npmjs.org

      # npm OIDC Trusted Publishing requires npm >= 11.5.1 (Node 20 ships npm 10).
      # Upgrade BEFORE corepack so a corepack `npm` shim can't shadow it; print to confirm.
      - name: Upgrade npm (Trusted Publishing needs npm >= 11.5.1)
        run: |
          npm install -g npm@latest
          npm --version

      # `corepack enable` (no args) also shims `npm` and would shadow the upgrade above.
      # Enable Yarn only.
      - name: Enable Corepack (Yarn)
        run: corepack enable yarn

      - name: Install dependencies
        run: yarn install

      - name: Build
        run: yarn build

      # OIDC Trusted Publishing (configured on npmjs.com for DiamondsLab/hardhat-diamonds):
      # no NPM_TOKEN needed — auth comes from the OIDC id-token; provenance is generated too.
      # `prepack: tsc --build --force` rebuilds a clean dist; publishConfig.access is public.
      - name: Publish (npm OIDC Trusted Publishing)
        run: |
          echo "npm: $(npm --version)"
          echo "OIDC id-token URL present: ${ACTIONS_ID_TOKEN_REQUEST_URL:+YES}"
          echo "registry: $(npm config get registry)"
          npm publish --provenance --access public
